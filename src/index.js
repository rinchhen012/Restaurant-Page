import {
    initialLoad,
    navBar
} from '../modules/initialLoad';
import {
    about
} from '../modules/about';

navBar();
initialLoad();

navBar.home.addEventListener("click", () => {})

navBar.about.addEventListener("click", () => {});